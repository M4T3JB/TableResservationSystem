package com.example.adriatik.repositories;

import com.example.adriatik.entities.SetReservation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SetReservationRepository extends JpaRepository<SetReservation, Integer> {
}